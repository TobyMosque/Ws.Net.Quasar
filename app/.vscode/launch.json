{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Program (Chrome)",
      "url": "https://localhost:7095",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
